name: "Add Labels"
description: "Add labels on Pull Requests or Issues"
inputs:
  issue_number:
    description: "The issue number"
    required: true
  labels:
    description: "Labels to add"
    required: true
runs:
  using: "node12"
  main: "dist/index.js"
branding:
  icon: "plus"
  color: "blue"
